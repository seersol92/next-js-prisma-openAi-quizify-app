import { Button } from '@/components/ui/button';

type Props = {}

const App = (props: Props) => {
  return (
    <div>     
      <Button variant={'default'}>Click me</Button>
    </div>
  )
}

export default App;